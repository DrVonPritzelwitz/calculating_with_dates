from datetime import datetime

dateOfBirth = input('Wat is je geboortedatum?\nVul in (dd-mm-yyyy): ')

def calculateAge(date):
	return 0

def calculateDaysToNextBirthDay(date):
	return 0

print('Je bent nu ' + str(calculateAge(dateOfBirth)) + ' jaar,')
print('over ' + str(calculateDaysToNextBirthDay(dateOfBirth)) + ' dag(en) ben je jarig! Whoot Whoot!')